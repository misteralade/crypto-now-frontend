import DashboardContent from "../sections/dashboard/DashboardContent.tsx";

// Standalone page wrapper (kept for legacy imports if any).
export default function DashboardPage() {
  return <DashboardContent />;
}
